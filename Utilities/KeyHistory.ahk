#SingleInstance Ignore
Persistent()
InstallKeybdHook()

; Double click this script's tray icon to open its main window.
; Press the desired key.
; Select the menu item [View > Key history].
; The most recent keys pressed will appear near the bottom of the page.
; If the desired key is detectable, look for the value in the first column.
; Your key can then be mapped as follows: vk**::
; Example (vk07::).